<h1>タスクの一覧</h1>

<ul>
    <% @tasks.each do |task|%>
        <li><%= link_to task.id, task %>：<%= task.status%> &gt; <%= task.content %></li>
    <% end %>
</ul>

<%= link_to '新規のタスクを作成する', new_task_path %>